{
  "name": "@ooon/tsconfig",
  "version": "0.0.0",
  "private": true,
  "description": "공용 TypeScript 설정 — 모든 Ooon 패키지가 상속한다.",
  "files": ["*.json", "README.md"],
  "exports": {
    "./base": "./base.json",
    "./library": "./library.json"
  }
}
